# import requests
# import json
# from datetime import datetime
# # from bot_app.models import ConnectedApi
# # #
# # # def api_checker(api_address, zip, tuser, a='fnc.api.zip.getdistance'):
# # #
# # #
# # #     url = '{api_address}?a={a}&zip={zip}&tuser={tuser}'.format(api_address=api_address, a=a, zip=zip, tuser=tuser)
# # #     request = requests.get(url)
# # #     if json.loads(request.text)['address']:
# # #         print('Zip найден у', api_address)
# # #     else:
# # #         print('Список пуст')
# # #
# # #
# # # api_checker(api_address='https://strongbox.cc/', zip=22222, tuser='D87hd487ft4')
# # #
# # #
# # #
# # # '?a=fnc.api.package.add&tuser={user_id}&zip={zipcode}&store_name={store_name}&store_phone={store_phone}&order_number={order_number}&pickup_person={pickup_person}&pickup_location={pickup_location}&more_information={more_info}&product_category={product_category}&product_item={product_item}&product_price={price}'
#
# date = datetime.utcfromtimestamp(t=)
# print(date)
from decimal import Decimal

a = Decimal(0.00000904)
print(len(a))